import Ember from 'ember'

export default Ember.Component.extend({
  classNames: ['col-md-3', 'col-xs-12', 'mix', 'webdev']
  // attributeBindings: ['style'],
  // style: 'height: 100%; border: 2px dotted black;'
})
